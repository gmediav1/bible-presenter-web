{
  "name": "bible-presenter-web",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "vite": "^7.1.3"
  },
  "dependencies": {
    "@netlify/functions": "^4.2.2"
  }
}
